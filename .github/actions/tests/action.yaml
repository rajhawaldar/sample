name: Test star

inputs:
  reportOnwer:
    description: "sample input"
    required: false
    default: "false"
  sec:
    description: "token"
    required: false

runs:
  using: composite
  steps:
    - name: print
      shell: bash
      run: echo "$reportOnwer"